apiVersion: v1
kind: Service
metadata:
  name: {{ include "blockchain-release-watcher.fullname" . }}
  labels:
    {{- include "blockchain-release-watcher.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "blockchain-release-watcher.selectorLabels" . | nindent 4 }}
